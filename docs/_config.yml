title: "LA 5580"
description: ""
remote_theme: "mmistakes/minimal-mistakes"

minimal_mistakes_skin: "air"

search: false

markdown: kramdown
permalink: pretty

collections:
  pages:
    output: true

defaults:
  - scope:
      path: ""
    values:
      layout: single
      sidebar:
        nav: "main"

plugins:
  - jekyll-include-cache
